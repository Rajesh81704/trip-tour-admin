# Backend API URL for development (uses local proxy routes)
NEXT_PUBLIC_DEV_BACKEND_URL=http://localhost:3000/api

# Backend API URL for production (uses local proxy routes)
NEXT_PUBLIC_PROD_BACKEND_URL=http://localhost:3000/api

# Actual backend server URL (server-side only)
BACKEND_URL=http://103.138.96.92:8000
